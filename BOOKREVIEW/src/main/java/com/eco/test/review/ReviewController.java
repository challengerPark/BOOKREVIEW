package com.eco.test.review;

import javax.servlet.http.HttpSession;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.web.multipart.MultipartFile;

@Controller
public class ReviewController {

	/*
		@Autowired
		//private ReviewService reviewService;
	
	
	
	
		//리뷰 등록
		public String insertReview(ReviewVo rv, MultipartFile[] upfile, int[] fileNumber, HttpSession session) {
			
			//rv.setUserNo(((MemberVo)session.getAttribute("loginUser")).getUserNo()); 
			
			int resultReview = 0;
			
			
			resultReview = reviewService.insertReview();
			
			
			return "";
		}
		
		
	*/
}
